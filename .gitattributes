# Shell scripts must keep LF endings — deploy.sh runs on the VPS via a
# `#!/usr/bin/env bash` shebang; a CRLF turns that into `bash\r` and the
# kernel can't find the interpreter (env: 'bash\r': No such file).
*.sh text eol=lf
